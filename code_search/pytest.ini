[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    fast: runs without GAP / ldpc / bposd / GPU
    gap: requires gappy + GAP + QDistRnd
    bposd: requires ldpc + bposd packages
    gpu: requires CUDA-capable device
