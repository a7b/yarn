"""Group-agnostic classical-filter implementations.

Single source of truth. The ``abelian/`` and ``non_abelian/`` sibling
folders contain one-line re-export shells per filter so callers can use
``search.filters.classical.{abelian,non_abelian}.<filter>`` and get the
same function.
"""
